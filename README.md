# Web-Study

This repository contains example HTML files and a simple Python script to display
an ASCII star animation in the terminal.

## Running the animation

From a command prompt with Python installed, run:

```
python star_animation.py
```

The script gradually draws a star using white lines and clears the screen
between frames.
